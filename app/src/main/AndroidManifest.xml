<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hiram.lavendimia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.NewClientActivity" />
        <activity android:name=".activities.NewArticleActivity" />
        <activity
            android:name=".activities.SelectClientActivity"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".activities.SelectProductsActivity" />
        <activity
            android:name=".activities.QuantityProductsActivity"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".activities.ShoppingCartActivity" />
        <activity android:name=".activities.FinalizeSaleActivity" />
        <activity android:name=".activities.SelectMonthActivity"></activity>
    </application>

</manifest>